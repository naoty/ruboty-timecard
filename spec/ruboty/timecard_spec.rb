require 'spec_helper'

describe Ruboty::Timecard do
  it 'has a version number' do
    expect(Ruboty::Timecard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
